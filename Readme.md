# Persistent State Agent Core

A lightweight, local stateful architecture designed to maintain context, mutate processing parameters over time, and persist across execution terminations using disk serialization.

## Execution Instructions
1. Ensure Python 3.10+ is installed on your local environment.
2. Install necessary dependencies:
   ```bash
   pip install -r requirements.txt
